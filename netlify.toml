# Netlify — site statique Ébrange Création Digital
[build]
  publish = "."
  command = ""

# Sécurité / cache basiques
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
